<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Калькулятор" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#2C3E50">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Калькулятор" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#ECF0F1"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,20"/>

        <!-- Input -->
        <StackPanel Grid.Row="1" Margin="30,10,30,10">
            <TextBlock Text="Выражение:" 
                       Foreground="#ECF0F1" 
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="ExpressionTextBox" 
                     Height="40" 
                     FontSize="16"
                     Padding="10,5,10,5"
                     Background="#FFFFFF"
                     BorderBrush="#BDC3C7"
                     BorderThickness="1"/>
        </StackPanel>

        <!-- Polish Display -->
        <StackPanel Grid.Row="2" Margin="30,10,30,10">
            <TextBlock Text="Обратная польская нотация:" 
                       Foreground="#ECF0F1" 
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="PolishNotationTextBox" 
                     Height="40" 
                     FontSize="16"
                     Padding="10,5,10,5"
                     Background="#F0F0F0"
                     BorderBrush="#BDC3C7"
                     BorderThickness="1"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="False"/>
        </StackPanel>

        <!-- Calculate Button -->
        <Button Grid.Row="3" 
                x:Name="CalculateButton"
                Content="Вычислить" 
                Height="50" 
                Width="200"
                FontSize="16"
                FontWeight="Bold"
                Background="#3498DB"
                Foreground="White"
                BorderBrush="#2980B9"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,20,0,0"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2980B9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#21618C"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Answer Label -->
        <StackPanel Grid.Row="3" 
                    VerticalAlignment="Bottom" 
                    Margin="30,0,30,30">
            <TextBlock Text="Ответ:" 
                       Foreground="#ECF0F1" 
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <Label x:Name="AnswerLabel" 
                   Height="40" 
                   FontSize="16"
                   FontWeight="Bold"
                   Background="#34495E"
                   Foreground="#ECF0F1"
                   BorderBrush="#2C3E50"
                   BorderThickness="1"
                   Content="0"
                   HorizontalContentAlignment="Left"
                   VerticalContentAlignment="Center"
                   Padding="10,0,10,0"/>
        </StackPanel>

    </Grid>
</Window>
